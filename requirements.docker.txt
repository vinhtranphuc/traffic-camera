# Core
opencv-python-headless>=4.8.0,<5
numpy>=1.24.0,<2
requests>=2.31.0,<3

# Web server
flask>=3.0.0,<4
waitress>=3.0.0,<4

# Detection & OCR (torch already installed as CPU-only in Dockerfile)
ultralytics>=8.0.0
easyocr>=1.7.0

# YouTube
yt-dlp>=2024.1.0
